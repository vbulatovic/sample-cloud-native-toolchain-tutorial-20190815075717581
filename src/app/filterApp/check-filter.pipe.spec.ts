import { CheckFilterPipe } from './check-filter.pipe';

describe('CheckFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
